# ==============================================================================
# FireGo 前端本地开发环境变量模板
# ==============================================================================
# 使用方法：
# 1. 复制此文件为 .env.local：cp .env.local.example .env.local
# 2. 运行 supabase start 获取本地 anon key
# 3. 将输出的 anon key 填入下方
# 4. 运行 npm run dev 启动前端
# ==============================================================================

# ==============================================================================
# 本地 Supabase 配置（覆盖 .env 中的生产配置）
# ==============================================================================

# 本地 Supabase API URL（固定值，不需要修改）
VITE_SUPABASE_URL=http://127.0.0.1:54321

# 本地 Supabase Anon Key
# 运行 supabase start 后，终端会输出类似：
#   anon key: eyJhbGciOiJIUzI1NiIs...
# 将该值复制到这里
VITE_SUPABASE_ANON_KEY=your_local_anon_key_here

# ==============================================================================
# 以下配置可选（本地开发时通常不需要修改）
# ==============================================================================

# Google 登录 Client ID（可使用生产环境的值）
# VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# Amplitude 统计（本地开发可留空）
# VITE_AMPLITUDE_API_KEY=your_amplitude_api_key

# Gemini API Key（用于前端 AI 交互）
# VITE_GEMINI_API_KEY=your_gemini_api_key
