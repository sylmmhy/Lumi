/**
 * AI Coach Session - 统一导出
 *
 * 使用方式：
 * import { useAICoachSession } from '../hooks/ai-coach';
 * import type { AICoachMessage } from '../hooks/ai-coach';
 */
export { useAICoachSession } from './useAICoachSession';
export type {
  AICoachMessage,
  AICoachSessionState,
  UseAICoachSessionOptions,
} from './types';
