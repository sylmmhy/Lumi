# ==============================================================================
# Supabase Edge Functions 本地开发环境变量模板
# ==============================================================================
# 使用方法：
# 1. 复制此文件为 .env.local：cp .env.local.example .env.local
# 2. 填入你自己的 API Keys
# 3. 运行 supabase start 启动本地开发环境
# ==============================================================================

# ==============================================================================
# AI 功能（必须配置）
# ==============================================================================

# Gemini API Key（用于 suggest-task-time）
# 获取地址: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Azure OpenAI（用于 memory-extractor）
# 获取地址: Azure Portal > Azure OpenAI Service
AZURE_AI_API_KEY=your_azure_ai_api_key_here
AZURE_AI_ENDPOINT=https://your-resource-name.openai.azure.com

# ==============================================================================
# iOS 推送通知（可选，仅测试推送功能时需要）
# ==============================================================================

# APNs (Apple Push Notification service)
# 获取地址: Apple Developer Console > Certificates, Identifiers & Profiles > Keys
APNS_KEY_ID=your_key_id
APNS_TEAM_ID=your_team_id
APNS_AUTH_KEY=your_auth_key_p8_content
APNS_BUNDLE_ID=com.mindboat.app

# ==============================================================================
# Android 推送通知（可选，仅测试推送功能时需要）
# ==============================================================================

# Firebase Cloud Messaging
# 获取地址: Firebase Console > Project Settings > Service Accounts
FCM_PROJECT_ID=your_firebase_project_id
FCM_CLIENT_EMAIL=your_service_account_email@project.iam.gserviceaccount.com
FCM_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----

# ==============================================================================
# OAuth（可选，本地测试 OAuth 登录时需要）
# ==============================================================================

# Google OAuth
# 获取地址: Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Apple OAuth
# 获取地址: Apple Developer Console
APPLE_CLIENT_ID=your_apple_client_id
APPLE_SECRET=your_apple_secret
